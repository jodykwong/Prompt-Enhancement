# 🔍 DeepSeek API 迁移验证指南

## 第一部分：环境准备验证

### 1.1 检查虚拟环境激活

```bash
# 激活虚拟环境
source venv/bin/activate

# 验证虚拟环境是否激活（应该看到 (venv) 前缀）
which python3
# 预期输出: /Users/jodykwong/Documents/augment-projects/Prompt-Enhancement/venv/bin/python3

# 检查 Python 版本
python3 --version
# 预期输出: Python 3.x.x
```

### 1.2 验证 .env 文件配置

```bash
# 检查 .env 文件是否存在
ls -la .env
# 预期输出: -rw-r--r-- ... .env

# 查看 .env 文件内容（仅显示密钥长度，不显示实际密钥）
cat .env | grep DEEPSEEK_API_KEY | wc -c
# 预期输出: 约 50+ 字符（包括变量名和密钥）

# 验证密钥格式（应该以 sk- 开头）
grep "^DEEPSEEK_API_KEY=sk-" .env && echo "✓ 密钥格式正确" || echo "✗ 密钥格式错误"
```

### 1.3 验证依赖安装

```bash
# 检查 openai 包是否安装
python3 -c "import openai; print(f'✓ openai 版本: {openai.__version__}')"
# 预期输出: ✓ openai 版本: 1.x.x

# 检查 python-dotenv 包是否安装
python3 -c "import dotenv; print('✓ python-dotenv 已安装')"
# 预期输出: ✓ python-dotenv 已安装

# 检查所有依赖
pip list | grep -E "openai|python-dotenv"
# 预期输出:
# openai                    1.x.x
# python-dotenv             1.x.x
```

---

## 第二部分：集成测试验证

### 2.1 运行集成测试

```bash
# 确保虚拟环境已激活
source venv/bin/activate

# 运行集成测试脚本
python3 test_deepseek_integration.py
```

### 2.2 预期的成功输出

```
================================================================================
DeepSeek API 集成测试
================================================================================

测试 1: API 密钥配置
  ✓ DEEPSEEK_API_KEY 已配置
  ✓ 密钥长度: 35 字符
  ✓ 密钥格式: sk-开头

测试 2: 导入检查
  ✓ openai 模块导入成功
  ✓ python-dotenv 模块导入成功
  ✓ 所有依赖可用

测试 3: 客户端初始化
  ✓ OpenAI 客户端初始化成功
  ✓ base_url 配置正确: https://api.deepseek.com
  ✓ API 密钥绑定成功

测试 4: API 调用测试
  ✓ 请求格式正确
  ✓ API 响应成功
  ✓ 返回数据有效
  ✓ 处理时间: 33.62 秒

================================================================================
总体结果: 4/4 通过 (100%)
================================================================================
```

### 2.3 故障排查

**如果测试失败，检查以下内容**:

```bash
# 1. 检查 API 密钥是否正确读取
python3 -c "
from dotenv import load_dotenv
import os
load_dotenv()
key = os.getenv('DEEPSEEK_API_KEY')
if key:
    print(f'✓ 密钥已读取，长度: {len(key)}')
else:
    print('✗ 密钥未找到')
"

# 2. 检查 OpenAI 客户端是否能初始化
python3 -c "
from openai import OpenAI
from dotenv import load_dotenv
import os
load_dotenv()
try:
    client = OpenAI(
        api_key=os.getenv('DEEPSEEK_API_KEY'),
        base_url='https://api.deepseek.com'
    )
    print('✓ 客户端初始化成功')
except Exception as e:
    print(f'✗ 客户端初始化失败: {e}')
"

# 3. 检查网络连接
python3 -c "
import socket
try:
    socket.create_connection(('api.deepseek.com', 443), timeout=5)
    print('✓ 网络连接正常')
except Exception as e:
    print(f'✗ 网络连接失败: {e}')
"
```

---

## 第三部分：真实 API 调用验证

### 3.1 简单的提示词增强测试

```bash


---

## 快速验证（推荐）

### 方法 1: 运行快速验证脚本（最简单）

```bash
# 进入项目目录
cd /Users/jodykwong/Documents/augment-projects/Prompt-Enhancement

# 运行快速验证脚本
bash QUICK_VERIFICATION.sh
```

**预期输出**:
```
================================================================================
  🔍 DeepSeek API 迁移快速验证
================================================================================

1️⃣  激活虚拟环境...
   ✓ 虚拟环境已激活

2️⃣  验证环境变量...
   ✓ DEEPSEEK_API_KEY 已配置
   ✓ 密钥长度: 35 字符
   ✓ 密钥格式正确 (sk- 开头)

3️⃣  验证依赖包...
   ✓ openai 已安装 (版本: 1.x.x)
   ✓ python-dotenv 已安装

4️⃣  运行集成测试...
   [集成测试输出...]

5️⃣  运行快速功能测试...
   测试提示词: '优化代码'
   ------------------------------------
   ✓ 增强成功
   ✓ 处理时间: 33.62 秒
   ✓ 原始长度: 4 字符
   ✓ 增强长度: 436 字符
   ✓ 扩展比例: 109x

================================================================================
  ✅ 快速验证完成！
================================================================================
```

### 方法 2: 运行完整验证脚本

```bash
# 激活虚拟环境
source venv/bin/activate

# 运行完整验证脚本
python3 verify_migration.py
```

**预期输出**:
```
================================================================================
  🔍 DeepSeek API 迁移完整验证
================================================================================

================================================================================
  第一部分：环境准备验证
================================================================================

1.1 虚拟环境检查
  ✓ 虚拟环境存在

1.2 .env 文件检查
  ✓ .env 文件存在

1.3 环境变量加载
  ✓ DEEPSEEK_API_KEY 已加载，长度: 35
  ✓ API 密钥格式正确 (sk- 开头)

1.4 依赖包检查
  ✓ openai 已安装，版本: 1.x.x
  ✓ python-dotenv 已安装

================================================================================
  第二部分：集成测试验证
================================================================================

2.1 API 密钥配置
  ✓ API 密钥已配置

2.2 导入检查
  ✓ OpenAI 导入成功

2.3 客户端初始化
  ✓ OpenAI 客户端初始化成功
  ✓ base_url 配置: https://api.deepseek.com

2.4 API 调用测试
  ✓ API 调用成功
  ✓ 返回有效响应，耗时: 2.34 秒

================================================================================
  第三部分：真实 API 调用验证
================================================================================

3.1 初始化增强器
  ✓ PromptEnhancer 初始化成功

3.2 测试提示词增强
  ✓ 增强成功，耗时: 33.62 秒
  ✓ 增强长度: 4 → 436 字符

================================================================================
  第四部分：功能验证
================================================================================

4.1 原意保持检查
  ✓ 包含关键词: 优化
  ✓ 包含关键词: 代码

4.2 输出质量检查
  ✓ 包含步骤
  ✓ 包含具体建议
  ✓ 长度合理
  ✓ 格式清晰

4.3 API 返回数据验证
  ✓ 字段 'original' 存在
  ✓ 字段 'enhanced' 存在
  ✓ 字段 'processing_time' 存在

================================================================================
  验证总结
================================================================================

  ✓ 环境准备: 通过
  ✓ 集成测试: 通过
  ✓ 真实 API 调用: 通过
  ✓ 功能验证: 通过

================================================================================
  ✅ 所有验证通过！系统已准备就绪。
================================================================================
```

---

## 常见问题排查

### 问题 1: DEEPSEEK_API_KEY 未找到

**症状**:
```
✗ DEEPSEEK_API_KEY 未找到
```

**解决方案**:
```bash
# 1. 检查 .env 文件是否存在
ls -la .env

# 2. 检查 .env 文件内容
cat .env | grep DEEPSEEK_API_KEY

# 3. 确保 .env 文件在项目根目录
pwd
ls -la .env
```

### 问题 2: openai 包未安装

**症状**:
```
✗ openai 未安装
```

**解决方案**:
```bash
# 激活虚拟环境
source venv/bin/activate

# 安装 openai
pip install openai>=1.0.0

# 验证安装
python3 -c "import openai; print(openai.__version__)"
```

### 问题 3: API 调用超时

**症状**:
```
✗ API 调用失败: timeout
```

**解决方案**:
```bash
# 1. 检查网络连接
ping api.deepseek.com

# 2. 检查防火墙
# 确保允许访问 api.deepseek.com:443

# 3. 检查 API 密钥是否有效
python3 << 'EOF'
from openai import OpenAI
from dotenv import load_dotenv
import os

load_dotenv()
client = OpenAI(
    api_key=os.getenv('DEEPSEEK_API_KEY'),
    base_url='https://api.deepseek.com'
)

try:
    response = client.chat.completions.create(
        model="deepseek-reasoner",
        messages=[{"role": "user", "content": "test"}],
        max_tokens=10
    )
    print("✓ API 连接成功")
except Exception as e:
    print(f"✗ API 连接失败: {e}")
EOF
```

### 问题 4: 处理时间过长

**症状**:
```
处理时间: 120+ 秒
```

**说明**: 这是正常的！DeepSeek-V3.2-Speciale 使用思考模式，处理时间通常为 30-40 秒。

**优化方案**:
- 添加缓存机制（避免重复调用）
- 使用异步处理
- 考虑使用标准 DeepSeek 模型（更快但质量略低）

---

## 验证完成后的下一步

✅ 所有验证通过后，您可以：

1. **集成到 Claude Code**
   - 将 `prompt_enhancer.py` 集成到您的应用中
   - 调用 `PromptEnhancer.enhance()` 方法

2. **监控 API 使用**
   - 跟踪 API 调用次数
   - 监控成本
   - 收集用户反馈

3. **优化性能**
   - 添加缓存机制
   - 实现异步处理
   - 考虑批量处理

4. **准备迁移计划**
   - 记录 2025-12-15 的截止日期
   - 评估是否继续使用特殊版本
   - 准备备选方案

---

**最后更新**: 2025-12-09
**验证工具版本**: 1.0
**状态**: ✅ 就绪

# 方法 1: 使用 Python 交互式命令
python3 << 'EOF'
from prompt_enhancer import PromptEnhancer
import json

enhancer = PromptEnhancer()
result = enhancer.enhance("优化代码")

print("=" * 80)
print("提示词增强测试结果")
print("=" * 80)
print(f"\n原始提示词: {result['original']}")
print(f"\n处理时间: {result['processing_time']:.2f} 秒")
print(f"\n增强后的提示词:\n{result['enhanced']}")
print("\n" + "=" * 80)
EOF
```

### 3.2 预期的输出格式

```
================================================================================
提示词增强测试结果
================================================================================

原始提示词: 优化代码

处理时间: 33.62 秒

增强后的提示词:
优化代码以提高性能、可读性和可维护性。请按以下步骤进行：

1. **性能分析：**
   - 使用性能分析工具（如 profiler）收集基准数据
   - 分析代码复杂度，评估算法和数据结构是否高效
   ...

================================================================================
```

### 3.3 多个测试用例

```bash
python3 << 'EOF'
from prompt_enhancer import PromptEnhancer
import time

enhancer = PromptEnhancer()

test_cases = [
    "修复 bug",
    "添加登录功能",
    "优化数据库查询",
    "重构代码"
]

print("=" * 80)
print("多个测试用例验证")
print("=" * 80)

for i, prompt in enumerate(test_cases, 1):
    print(f"\n测试 {i}: {prompt}")
    print("-" * 40)

    try:
        result = enhancer.enhance(prompt)
        print(f"✓ 成功")
        print(f"  处理时间: {result['processing_time']:.2f} 秒")
        print(f"  增强长度: {len(result['enhanced'])} 字符")
    except Exception as e:
        print(f"✗ 失败: {e}")

print("\n" + "=" * 80)
EOF
```

---

## 第四部分：功能验证

### 4.1 验证原意保持

```bash
python3 << 'EOF'
from prompt_enhancer import PromptEnhancer

enhancer = PromptEnhancer()

# 测试原意保持
test_prompt = "优化代码"
result = enhancer.enhance(test_prompt)

# 检查增强后的提示词是否包含原始关键词
enhanced = result['enhanced'].lower()
original_keywords = ["优化", "代码"]

print("原意保持检查:")
for keyword in original_keywords:
    if keyword in enhanced:
        print(f"  ✓ 包含关键词: {keyword}")
    else:
        print(f"  ✗ 缺少关键词: {keyword}")
EOF
```

### 4.2 验证输出质量

```bash
python3 << 'EOF'
from prompt_enhancer import PromptEnhancer

enhancer = PromptEnhancer()
result = enhancer.enhance("优化代码")

# 质量检查
enhanced = result['enhanced']
checks = {
    "包含步骤": "步骤" in enhanced or "1." in enhanced,
    "包含具体建议": "工具" in enhanced or "方法" in enhanced,
    "包含最佳实践": "最佳实践" in enhanced or "原则" in enhanced,
    "长度合理": len(enhanced) > 200,
    "格式清晰": "\n" in enhanced
}

print("输出质量检查:")
for check, result in checks.items():
    status = "✓" if result else "✗"
    print(f"  {status} {check}")
EOF
```

### 4.3 验证 API 调用成功

```bash
python3 << 'EOF'
from prompt_enhancer import PromptEnhancer
import json

enhancer = PromptEnhancer()
result = enhancer.enhance("优化代码")

# 验证返回数据结构
required_fields = ['original', 'enhanced', 'processing_time']

print("API 返回数据验证:")
for field in required_fields:
    if field in result:
        print(f"  ✓ 字段 '{field}' 存在")
    else:
        print(f"  ✗ 字段 '{field}' 缺失")

# 验证数据类型
print("\n数据类型验证:")
print(f"  original: {type(result['original']).__name__} ✓")
print(f"  enhanced: {type(result['enhanced']).__name__} ✓")
print(f"  processing_time: {type(result['processing_time']).__name__} ✓")
EOF
```

---

## 验证清单

- [ ] 虚拟环境已激活
- [ ] .env 文件已配置
- [ ] DEEPSEEK_API_KEY 格式正确
- [ ] openai 包已安装
- [ ] python-dotenv 包已安装
- [ ] 集成测试 4/4 通过
- [ ] 简单 API 调用成功
- [ ] 原意保持检查通过
- [ ] 输出质量检查通过
- [ ] API 返回数据有效

---

**验证完成时间**: 2025-12-09
**验证状态**: 准备就绪

